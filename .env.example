# Local development (default: PocketBase on localhost:8090 via Vite proxy)
# You do NOT need to set this for local dev — the Vite proxy handles it.
# VITE_PB_URL=http://127.0.0.1:8090

# Production: set this to your deployed PocketBase URL
# VITE_PB_URL=https://your-pocketbase.railway.app
